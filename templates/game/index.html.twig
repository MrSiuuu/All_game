{% extends 'base.html.twig' %}

{% block title %}Tous les jeux{% endblock %}

{% block body %}
<div class="container mx-auto py-8">
    <h1 class="text-2xl text-white font-bold mb-6">Tous les jeux 🎮</h1>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {% for game in games %}
            <div class="bg-gray-800 p-4 rounded shadow">
                <h2 class="text-xl text-white font-semibold">{{ game.name }}</h2>
                <p class="text-gray-400">{{ game.price }} €</p>
                <p class="text-sm text-gray-300">{{ game.description|length > 100 ? game.description[:100] ~ '...' : game.description }}</p>
                <p class="text-xs text-gray-500">Publié le {{ game.releaseDate|date('d/m/Y') }}</p>
            </div>
        {% else %}
            <p class="text-gray-400">Aucun jeu trouvé.</p>
        {% endfor %}
    </div>
</div>
{% endblock %}
